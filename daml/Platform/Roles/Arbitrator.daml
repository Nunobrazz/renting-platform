module Platform.Roles.Arbitrator where

template Arbitrator
  with
    operator : Party
    individual : Party
  where
    signatory operator
    observer individual
    key (operator, individual) : (Party, Party)
    maintainer key._1








verifyArbitrator : Party -> Party -> Update ()
verifyArbitrator operator individual = do fetchByKey @Arbitrator (operator, individual); pure()